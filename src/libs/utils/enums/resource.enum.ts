export enum Resource {
    contacts = 'contacts',
    users = 'users',
    recordings = 'recordings',
    appointments = 'appointments',
    settings = "settings",
    dashhboard = "dashboard",
    overview = "overview",
    roles = "roles",
}